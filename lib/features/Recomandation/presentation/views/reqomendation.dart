import 'package:flutter/material.dart';

class Reqomendation extends StatelessWidget {
  const Reqomendation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}